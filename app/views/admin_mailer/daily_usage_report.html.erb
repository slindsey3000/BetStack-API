<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BetStack API Daily Report</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    h1 { color: #667eea; font-size: 24px; margin-bottom: 5px; }
    h2 { color: #333; font-size: 18px; margin-top: 30px; margin-bottom: 15px; border-bottom: 2px solid #667eea; padding-bottom: 5px; }
    .subtitle { color: #666; font-size: 14px; margin-bottom: 30px; }
    .stat-grid { display: flex; flex-wrap: wrap; gap: 15px; margin-bottom: 20px; }
    .stat-box { background: #f8f9fa; border-radius: 8px; padding: 15px; min-width: 120px; flex: 1; }
    .stat-value { font-size: 28px; font-weight: bold; color: #667eea; }
    .stat-label { font-size: 12px; color: #666; text-transform: uppercase; }
    table { width: 100%; border-collapse: collapse; margin: 15px 0; }
    th, td { padding: 10px; text-align: left; border-bottom: 1px solid #eee; }
    th { background: #f8f9fa; font-weight: 600; font-size: 12px; text-transform: uppercase; color: #666; }
    .number { text-align: right; font-family: monospace; }
    .warning { color: #e74c3c; }
    .success { color: #27ae60; }
    .footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid #eee; font-size: 12px; color: #888; }
  </style>
</head>
<body>
  <h1>📊 BetStack API Daily Report</h1>
  <p class="subtitle"><%= @date.strftime('%A, %B %d, %Y') %></p>

  <h2>👥 Client API Usage</h2>
  <p style="color: #666; font-size: 14px;">Requests from API key holders to our API</p>
  
  <div class="stat-grid">
    <div class="stat-box">
      <div class="stat-value"><%= number_with_delimiter(@client_today) %></div>
      <div class="stat-label">Requests Yesterday</div>
    </div>
    <div class="stat-box">
      <div class="stat-value <%= @client_rejected > 0 ? 'warning' : 'success' %>"><%= number_with_delimiter(@client_rejected) %></div>
      <div class="stat-label">Rejected (Rate Limited)</div>
    </div>
    <div class="stat-box">
      <div class="stat-value"><%= number_with_delimiter(@client_month) %></div>
      <div class="stat-label">This Month</div>
    </div>
  </div>

  <% if @client_by_email.any? %>
  <h3 style="font-size: 14px; color: #666;">Top Users Yesterday</h3>
  <table>
    <thead>
      <tr>
        <th>Email</th>
        <th class="number">Requests</th>
        <th class="number">Rejected</th>
      </tr>
    </thead>
    <tbody>
      <% @client_by_email.each do |user| %>
      <tr>
        <td><%= user[:email] %></td>
        <td class="number"><%= number_with_delimiter(user[:requests]) %></td>
        <td class="number <%= user[:rejected].to_i > 0 ? 'warning' : '' %>"><%= number_with_delimiter(user[:rejected] || 0) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
  <p style="color: #888;">No client requests yesterday.</p>
  <% end %>

  <% if @top_abusers.any? %>
  <h3 style="font-size: 14px; color: #e74c3c;">⚠️ Abuse Detected</h3>
  <table>
    <thead>
      <tr>
        <th>Email</th>
        <th class="number">Rejected Requests</th>
      </tr>
    </thead>
    <tbody>
      <% @top_abusers.each do |abuser| %>
      <tr>
        <td><%= abuser[:email] %></td>
        <td class="number warning"><%= number_with_delimiter(abuser[:rejected]) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>

  <h2>🔌 Internal API Usage</h2>
  <p style="color: #666; font-size: 14px;">Our requests to The Odds API (data provider)</p>
  
  <div class="stat-grid">
    <div class="stat-box">
      <div class="stat-value"><%= number_with_delimiter(@internal_today) %></div>
      <div class="stat-label">Requests Yesterday</div>
    </div>
    <div class="stat-box">
      <div class="stat-value"><%= number_with_delimiter(@internal_month) %></div>
      <div class="stat-label">This Month</div>
    </div>
    <div class="stat-box">
      <div class="stat-value"><%= number_to_percentage((@internal_month.to_f / 100000 * 100), precision: 1) %></div>
      <div class="stat-label">Quota Used</div>
    </div>
  </div>

  <% if @internal_by_league.any? %>
  <h3 style="font-size: 14px; color: #666;">By League Yesterday</h3>
  <table>
    <thead>
      <tr>
        <th>League</th>
        <th class="number">Requests</th>
      </tr>
    </thead>
    <tbody>
      <% @internal_by_league.each do |league, count| %>
      <tr>
        <td><%= league %></td>
        <td class="number"><%= number_with_delimiter(count) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>

  <h2>📈 User Stats</h2>
  <div class="stat-grid">
    <div class="stat-box">
      <div class="stat-value"><%= @total_users %></div>
      <div class="stat-label">Total Users</div>
    </div>
    <div class="stat-box">
      <div class="stat-value"><%= @active_users %></div>
      <div class="stat-label">Active Users</div>
    </div>
    <div class="stat-box">
      <div class="stat-value <%= @new_users_today > 0 ? 'success' : '' %>"><%= @new_users_today %></div>
      <div class="stat-label">New Yesterday</div>
    </div>
  </div>

  <div class="footer">
    <p>Generated at <%= @generated_at.strftime('%B %d, %Y at %I:%M %p %Z') %></p>
    <p>BetStack API &bull; <a href="https://api.betstack.dev/usage">View Dashboard</a></p>
  </div>
</body>
</html>

